{
  "name": "bc-minimes",
  "private": true,
  "type": "module",
  "scripts": {
    "_note_prepare": "npm install branche le crochet qui retire les signatures d'outil des messages de commit (voir .githooks/commit-msg)",
    "prepare": "git config core.hooksPath .githooks",
    "dev": "astro dev",
    "prebuild": "node scripts/avis-google.mjs",
    "build": "astro build",
    "verifier-agents": "node scripts/verifier-agents.mjs",
    "postbuild": "node scripts/cuire-galerie.mjs && node scripts/cuire-pages.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "cloudinary": "^2.5.0"
  }
}
